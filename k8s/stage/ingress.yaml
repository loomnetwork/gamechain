---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: dev-gamechain-oracle
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
  - host:   stage-gcoracle.devdc.io
    http:
      paths:
      - path: /status, /metrics
        backend:
          serviceName: stage-gamechain-oracle-svc
          servicePort: main-port  